version: '3.1'

services:
    adminer:
        image: {{ adminer.image }}
        restart: always
{% if adminer.ports is defined %}
        ports:
            {{ adminer.ports | to_nice_yaml }}
{% endif %}
